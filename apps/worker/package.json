{
  "name": "@walour/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsup src/scan.ts src/decode.ts src/ingest.ts src/promote.ts src/purge.ts src/blink.ts --format cjs --outDir api --no-dts --no-splitting",
    "dev": "tsx watch --env-file=.env src/server.ts",
    "deploy": "vercel deploy --prod",
    "seed:chainabuse": "tsx --env-file=.env scripts/seed-chainabuse-csv.ts"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/web3.js": "^1.98.0",
    "@supabase/supabase-js": "^2.43.0",
    "@walour/sdk": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0"
  }
}
